[pytest]

python_files = test_*.py

markers =
    validate_csv: marks tests for CSV validation
    unmarked: automatically assigned to tests without explicit marks

addopts = --html=report.html --self-contained-html